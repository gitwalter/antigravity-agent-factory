# Core dependencies for Antigravity Agent Factory# Install with: pip install -r requirements.txt
#
# For development dependencies (testing, linting), see requirements-dev.txt

# =============================================================================
# Core Dependencies
# =============================================================================

# YAML support for configuration files
pyyaml>=6.0.0

# Template engine for code generation
Jinja2>=3.1.0

# Async HTTP client for adapters
aiohttp>=3.9.0

# Schema validation for JSON files
jsonschema>=4.17.0

# =============================================================================
# Memory System (Inductive Learning)
# =============================================================================

# Local embeddings for semantic memory (no API key required)
# Downloads ~80MB model on first use
sentence-transformers>=2.2.0

# Vector database client for memory store
qdrant-client>=1.7.0

# NumPy (required by sentence-transformers and qdrant-client)
numpy>=1.24.0

# =============================================================================
# AI & Agents
# =============================================================================

# LangChain Ecosystem
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.10
langgraph>=0.0.10
langchain-huggingface>=0.0.1
langchain-qdrant>=0.0.1
langchain-text-splitters>=0.0.1

# PDF Processing
pymupdf>=1.20.0

# Data Validation
pydantic>=2.0.0
