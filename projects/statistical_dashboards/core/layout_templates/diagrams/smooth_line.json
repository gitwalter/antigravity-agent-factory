{
    "title": "{{ title|default('Trend Analysis') }}",
    "labels": {
        "x": "{{ x_label|default('Date') }}",
        "y": "{{ y_label }}"
    },
    "template": "plotly_dark",
    "line_shape": "spline"
}
