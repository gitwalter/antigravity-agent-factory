# Development dependencies for Antigravity Agent Factory
# Install with pip (see .agent/config/tools.json for configured path)
# Default Windows: C:\App\Anaconda\Scripts\pip.exe install -r requirements-dev.txt
# Linux/macOS: pip install -r requirements-dev.txt

# Testing framework
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-xdist>=3.5.0  # Parallel test execution
pytest-timeout>=2.2.0  # Test timeout enforcement

# Schema validation for JSON files
jsonschema>=4.17.0

# YAML support (also needed by main project)
pyyaml>=6.0.0

# Template engine
Jinja2>=3.1.0

# Optional: for better test output
pytest-sugar>=0.9.6

# Linting (used by CI pipeline)
ruff>=0.4.0

# Dependency graph validation
packaging>=23.0  # PyPA standard for version parsing (PEP 440)

# =============================================================================
# Memory System (Inductive Learning)
# =============================================================================

# Local embeddings for semantic memory (no API key required)
sentence-transformers>=2.2.0

# Vector database for memory storage
chromadb>=0.4.0
