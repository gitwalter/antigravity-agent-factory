{#
Template Metadata (schema: template.schema.json)
name: mcp-config-local.json
description: Generates mcp config local.json file for tools development projects
version: 1.0.0
category: tools
produces: JSON data file
variables:
    - project_name: (string) The project name [required]
used_by_skills: [tool-usage, mcp-integration]
used_by_blueprints: [langchain-agent-platform]
#}
{
  "mcpServers": {
    "atlassian-local": {
      "command": "node",
      "args": [
        "${MCP_ATLASSIAN_LOCAL_PATH}/dist/index.js"
      ],
      "env": {
        "ATLASSIAN_API_TOKEN": "${ATLASSIAN_API_TOKEN}",
        "ATLASSIAN_DOMAIN": "${ATLASSIAN_DOMAIN}",
        "ATLASSIAN_EMAIL": "${ATLASSIAN_EMAIL}",
        "ATLASSIAN_BASE_URL": "${ATLASSIAN_BASE_URL}"
      }
    }
  }
}
